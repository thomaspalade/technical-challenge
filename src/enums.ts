export enum SUPPORTED_LANGUAGES {
  CHINESE = 'zh',
  ENGLISH = 'en-gb',
  GERMAN = 'de',
}

export enum ERROR_MESSAGES {
  CACHED_DATA_NOT_AVAILABLE = 'Cached data not available',
  DATA_NOT_AVAILABLE = 'Data could not be retrieved from URL',
  EVENT_NOT_FOUND = 'Event not found',
  LANGUAGE_NOT_SUPPORTED = 'Language not supported yet',
  SPORTS_NOT_FOUND = 'Sports not found for the current language',
}
