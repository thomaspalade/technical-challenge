import test from 'ava';

test('When toFailure is called, Then it returns 200', async (t) => {
  t.true(true);
});

test('When toSuccess is called, Then it returns 200', async (t) => {
  t.true(true);
});

test('When isSupportedLanguage is called, Then it returns 200', async (t) => {
  t.true(true);
});
