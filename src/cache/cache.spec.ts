import test from 'ava';

test('When cleanUpCache is called, Then it returns 200', async (t) => {
  t.true(true);
});

test('When cacheContainsDataForLanguage is called, Then it returns 200', async (t) => {
  t.true(true);
});

test('When getVictorBetResult is called, Then it returns 200', async (t) => {
  t.true(true);
});

test('When getCacheData is called, Then it returns 200', async (t) => {
  t.true(true);
});
